#include <bits/stdc++.h>

using i64 = long long;

#define LOG(...) std::cerr << "DEBUG: " << __VA_ARGS__ << std::endl;

void solve() {
    int n;
    std::cin >> n;
    for(int i = 0; i < n; i++) {
        std::cout << "!qiandao easy!" << std::endl;
        
    }
    
}

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);
    solve();
    return 0;
}