#include <bits/stdc++.h>

using i64 = long long;

void solve() {
    std::cout << 0 << '\n';
    std::cout.flush();
    i64 sum;
    std::cin >> sum; // x + y
}

int main() {
    //std::cin.tie(nullptr)->sync_with_stdio(false);
    int t; std::cin >> t; while (t--) solve();
    return 0;
}