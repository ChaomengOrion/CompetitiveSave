#include <bits/stdc++.h>

void solve()
{
    int X, A;
    std::cin >> X >> A;
    if (X < A) {
        std::cout << 0 << std::endl;
    } else {
        std::cout << 10 << std::endl;
    }
}

int main()
{
    std::cin.tie(nullptr)->sync_with_stdio(false);
    solve();
}