#include <bits/stdc++.h>

void solve()
{
    int A, P;
    std::cin >> A >> P;
    std::cout << (A * 3 + P) / 2 << std::endl;
}

int main()
{
    std::cin.tie(nullptr)->sync_with_stdio(false);
    solve();
    return 0;
}